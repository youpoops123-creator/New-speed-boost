local UIS = game:GetService("UserInputService")

-- ScreenGui
local gui = Instance.new("ScreenGui", game.CoreGui)
gui.Name = "JAIMEPASARBI_HUB"

-- Main Frame
local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 420, 0, 260)
main.Position = UDim2.new(0.5, -210, 0.5, -130)
main.BackgroundColor3 = Color3.fromRGB(15,15,15)
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true

-- Corner
local corner = Instance.new("UICorner", main)
corner.CornerRadius = UDim.new(0, 14)

-- Glow
local stroke = Instance.new("UIStroke", main)
stroke.Color = Color3.fromRGB(170, 0, 255)
stroke.Thickness = 2

-- Title
local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 40)
title.BackgroundTransparency = 1
title.Text = "JAIMEPASARBI HUB"
title.TextColor3 = Color3.fromRGB(200,200,200)
title.Font = Enum.Font.GothamBold
title.TextSize = 20

-- Button function
function createButton(text, posY)
    local btn = Instance.new("TextButton", main)
    btn.Size = UDim2.new(0, 360, 0, 40)
    btn.Position = UDim2.new(0.5, -180, 0, posY)
    btn.BackgroundColor3 = Color3.fromRGB(25,25,25)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(220,220,220)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 16

    local c = Instance.new("UICorner", btn)
    c.CornerRadius = UDim.new(0, 10)

    local s = Instance.new("UIStroke", btn)
    s.Color = Color3.fromRGB(170, 0, 255)

    return btn
end

-- Buttons
local finder = createButton("Finder OP", 60)
finder.MouseButton1Click:Connect(function()
    print("Finder activé")
end)

local farm = createButton("Auto Farm", 110)
farm.MouseButton1Click:Connect(function()
    print("Auto farm activé")
end)

local hop = createButton("Server Hop", 160)
hop.MouseButton1Click:Connect(function()
    print("Server hopping...")
end)

local discord = createButton("Discord", 210)
discord.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/tonserveur")
end)
